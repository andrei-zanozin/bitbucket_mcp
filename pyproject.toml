[project]
  name = "bitbucket-mcp"
  version = "0.1.0"
  description = "Minimal Bitbucket Data Center MCP server."
  requires-python = ">=3.11"
  dependencies = [
      "httpx>=0.28,<1",
      "mcp>=2.1.1,<3",
      "pydantic>=2,<3",
      "PyYAML>=6,<7",
  ]

  [tool.uv]
  package = false